'use client'

// This component is now just for styling purposes
// Botpress will inject its own floating button
// We keep this as a fallback or alternative UI element
export function BotpressLauncher() {
  return null
}

